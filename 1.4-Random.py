#1.4 Random in Python

#----------------------------------------

#Python cannot gennerate random numbers
#However, it has a built-in module named random()
#To use a module in Python, we have to import the module

import random

#Generate a random number from 1 to 10
#Use randrange(start_point, end_point) method in random to generate numbers
a = random.randrange(1,10)
print (a)

